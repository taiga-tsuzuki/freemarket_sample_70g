.wrapper
  .header
    %h2
      購入を確定しますか？



  .main
    %p
      = @item.item_name

    %p.item__price
      = @item.price
    %br
    %h3
      支払い方法
    - if @default_card_information.blank?
      %br /
    - else
      -#以下カード情報を表示
      = "**** **** **** " + @default_card_information.last4
      - exp_month = @default_card_information.exp_month.to_s
      - exp_year = @default_card_information.exp_year.to_s.slice(2,3)
      %br/
      有効期限
      %br/
      = exp_month + " / " + exp_year
    %br
    = link_to pay_purchase_path(@item.id),class: "purchase" do
      購入を確定する
    = link_to confirm_item_path(@item.id),class: "return" do
      戻る